/** Package invariant companion for the settings-backed SSL certificate plugin. */
export const name = 'client-ui-ssl-certificates-invariant'

/** This plugin owns a settings namespace; lifecycle correctness is covered by the settings service. */
export function apply(): void {}
